#include "lists.h"
/**
* reverse_listint - This function reverses a linked list
* @head: a pointer to the pointer of the first node of the linked list
* Return: a pointer to thr first node of the reversed list
*/
listint_t *reverse_listint(listint_t **head)
{
	listint_t *ptr, *optr;
	/**
	*if (*head == NULL || (*head)->next == NULL)
	*	return (*head);
	*optr = reverse_listint(&((*head)->next));
	*ptr->next->next = optr;
	*ptr
	*/

/**
*	ptr = *head;
*	*optr = NULL;
*
*	while (ptr != NULL)
*	{
*
*	}
*/
}
