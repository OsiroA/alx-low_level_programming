#include "main.h"
/* This function checks for lowercase character*/
/**
 * _islower() - Checks for lowercase characters
 *
 * Return: 0
 */

int _islower(int c)
{
	int c;

	for (c = 'a'; c <= 'z'; c++)
	{
		_putchar(1);
	for (c = 'A'; c <= 'Z'; c++)
	{
		_putchar(0);
	}
	}
	
	return (int);
}
