#include <stdio.h>
/*This program prints the alphavbbet in lower case*/

/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	putchar(c);
	putchar('\n');

return (0);
}
