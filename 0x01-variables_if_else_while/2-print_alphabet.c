#include <stdio.h>

/**
 * This program prints the alphabet in lowercase
 *
 * main - Entry point
 *
 * Retirn: 0 (Success)
 */

int main(void)
{
	char c;
	c = 'a';

	if (c <= z)
		putchar("c\n");
		c++;

	return (0);
}
