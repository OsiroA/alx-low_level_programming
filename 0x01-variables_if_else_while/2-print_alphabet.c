#include <stdio.h>

/**
 * This program prints the alphabet in lowercase
 *
 * main - Entry point
 *
 * Retirn: 0 (Success)
 */

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++);
		putchar(c);
		putchar('\n');

return (0);
}
