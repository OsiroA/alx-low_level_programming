#include <stdio.h>
/* This program prints all single digit numbers of base 10*/

/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */

int main()
{
	int a = 0;

	if (a < 10)
		printf("%d\n", a);

return (0);
}
