#include <stdio.h>
/* This program prints all single digit numbers of base 10*/

/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */

int main(void)
{
	int a = 0;

	for (a = 0; a <10; a++)
	printf("%d", a);
	printf("\n");

return (0);
}
