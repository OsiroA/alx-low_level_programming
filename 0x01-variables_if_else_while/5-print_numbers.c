#include <stdio.h>
/* This program prints all single digit numbers of base 10*/

/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */

int main(void)
{
	int a = 0;

	if (a < 10)
	{
		printf("%i", a);
		a++;
		printf("\n");
	}

return (0);
}
